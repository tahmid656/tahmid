<!DOCTYPE html>
<html>
  <head>
    <title>Tapir Grocer</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <%= render "shared/header" %>
    <% if notice.present?%>
      <div class="notification is-link is-light"><%= notice %></p>
    <% end %>
    <% if alert.present?%>
      <div class="notification is-danger is-light"><%= alert %></p>
    <% end %>
    <div class ="container">
      <%= yield %>
    </div>
  </body>
</html>
